package training.designpatterns.mapbasedfactory.product;

public interface Product {
/**
 * this interface just share the type name along product types
 * no fields, so better than abstract class
 */
}
